{
  "name": "pkm",
  "private": true,
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "build": "npm run build -w apps/web",
    "typecheck": "tsc --noEmit -p packages/core-data/tsconfig.json && tsc --noEmit -p packages/core-ui/tsconfig.json && tsc --noEmit -p apps/web/tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
